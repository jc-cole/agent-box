[agents.codex]
args = []
cmd = 'codex exec --yolo'
mode = 'host'

[project]
default_test = 'make test'

[workspace]
log_dir = '.agentbox/logs'
task_dir = '.agentbox/tasks'
worktree_dir = '.agentbox/worktrees'
